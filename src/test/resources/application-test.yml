spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:54320/naeil_bank_test
    username: naeil
    password: naeil_test
  flyway:
    enabled: true
    locations: classpath:db/migration
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
    show-sql: false

app:
  jwt:
    secret: test-jwt-secret-fixture-32-bytes-minimum
    access-token-ttl: 30m
    refresh-token-ttl: 14d
  cors:
    allow-credentials: true
    allowed-origins:
      - http://localhost:5173
      - http://localhost:3000
    allowed-methods:
      - GET
      - POST
      - PUT
      - PATCH
      - DELETE
      - OPTIONS
    allowed-headers:
      - Authorization
      - Content-Type
    exposed-headers:
      - Location
  scheduling:
    morning-statement-cron: "0 0 8 * * *"
  upload:
    max-input-size: 10MB
    max-generated-size: 20MB
  timezone:
    default-zone: Asia/Seoul

kakao:
  client-id: test
  redirect-uri: http://localhost/test
  client-secret: test

openai:
  api-key: test-openai-api-key-fixture
  meal-model: gpt-5-mini
  face-model: gpt-5-mini
  timeout: 30s

web-push:
  vapid:
    public-key: test-vapid-public-key-fixture
    private-key: test-vapid-private-key-fixture
    subject: mailto:test@example.com
